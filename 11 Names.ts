//Name: Store the names of a few of your friends in a array called names.
//Print each person's name by accessing each element in the list.
//One at a time.
let names :string[ ] = ['Naveed','Ali','Mateen'];
for(let i=0; i<names.length;i++){
    console.log(names[i]);
}